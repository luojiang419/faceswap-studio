name: faceswap_studio
description: "FaceSwap Studio Flutter desktop shell."
publish_to: 'none'
version: 0.1.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  file_selector: ^1.1.0
  http: ^1.5.0
  image: ^4.8.0
  media_kit: ^1.2.6
  media_kit_libs_video: ^1.0.7
  media_kit_video: ^2.0.1
  super_clipboard: ^0.9.1
  webview_all: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
