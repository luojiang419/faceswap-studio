# 核心服务更新启动修复

状态：已完成
当前阶段：5/5
最后更新：2026-09-03

## 当前状态

FaceSwap Studio 安装目录为 `D:\Program Files\FaceSwap Studio`。今天应用 FaceFusion 3.8.3 核心更新后，Bridge 无法导入并退出，启动器因此报告 Bridge failed to start。已从安装目录日志确认根因是 Bridge 仍依赖项目兼容层 `get_output_file_extension`，而上游 3.8.3 的 `facefusion.filesystem` 已移除该函数。Bridge 两个 worker 也必须使用 3.8.x 的 `face_creator` API。

仓库工作区原本已有大量未提交升级改动，已保留，不执行回退或强制覆盖。本轮已将 FaceFusion 3.8.3 核心纳入源码基线，并补齐 Bridge 兼容层。复测发现安装版 Flutter 构建产物仍硬编码旧端口 `50941`，已重新构建 Web/Windows 产物并同步安装目录。

## 下一步

1. [已完成] 在 Bridge/核心兼容层修复 3.8.3 导入和调用兼容性。
2. [已完成] 使用仓库虚拟环境执行 Bridge 导入、相关 pytest 与启动器健康检查。
3. [已完成] 将修复后的核心与 Bridge 文件同步到 `D:\Program Files\FaceSwap Studio`，保留安装目录运行数据。
4. [已完成] 从安装目录实际启动并验证 Bridge `/health`、FaceFusion 状态和前端进程。
5. [已完成] 重建 Flutter Web/Windows 产物并修复安装版端口不一致。

## 当前 TODO

- [x] 修复 Bridge 对 FaceFusion 3.8.3 的兼容性
- [x] 补充回归测试或兼容性断言（现有 Bridge/worker 回归覆盖）
- [x] 更新安装目录
- [x] 实际启动验证
- [x] 更新本任务文档、Git 状态和提交记录

## 最近验证状态

- 静态检查：`compileall` 通过，`git diff --check` 仅有换行符提示和既有 EOF 空行提示
- 单元测试：Bridge/worker/launcher 相关 `49 passed, 1 warning`
- 编译：`scripts/build_flutter_app.ps1` 成功，Web/Windows 产物已同步
- 运行测试：安装目录启动器运行中，Bridge `/health` 200、FaceFusion `ready`、前端进程存活
- 最近 Git commit：待提交本轮核心更新兼容修复

---

## 任务目标

让 FaceFusion 3.8.3 核心更新后 Bridge、FaceFusion 和 FaceSwap Studio 均可正常启动，并把修复同步到实际安装目录完成运行验证。

## 当前项目现状

- 源码仓库：`G:\app\facefusion\faceswap-studio`
- 安装目录：`D:\Program Files\FaceSwap Studio`
- 核心更新缓存：`C:\Users\jiang\AppData\Local\FaceSwap Studio\updates\facefusion-core\3.8.3`
- Bridge 入口：`faceswap studio/bridge/app_server.py`
- 启动器：`launch_faceswap_studio.py`
- 关键兼容点：Bridge 使用 `get_output_file_extension`；上游 3.8.3 删除该函数；worker 需从 `facefusion.face_creator` 获取人脸。

## 技术方案

在 Bridge 内保留上游 API 删除后的兼容 shim，避免核心更新再次造成导入失败；将 3.8.3 核心、Bridge 和 worker 同步到安装目录，使用真实 Python/Bridge 进程做启动验证。

## 验收标准

- [x] 仓库虚拟环境可导入 `faceswap studio/bridge/app_server.py`。
- [x] 相关 Bridge 测试通过。
- [x] 安装目录 Bridge 能在配置端口监听并返回 `/health` HTTP 200。
- [x] 安装目录启动器不再因 Bridge 导入失败退出，FaceFusion 状态可查询。
- [x] 更新后的文件和验证结果写入本任务文档；Git 状态已检查。

## 已完成内容

- FaceFusion 核心版本更新为 3.8.3。
- Bridge 增加 `get_output_file_extension` 本地兼容 shim，保持 ProRes `.mov` 输出。
- Bridge 两个工作进程切换到 `face_creator` API 和 `default` UI 布局。
- 安装目录核心、Bridge、worker、启动器已同步；runtime 数据保留。
- 重新构建并同步 Flutter Web/Windows 产物，安装版 `main.dart.js` 已从端口 `50941` 更新为 `52741`。
- 仓库与安装目录均完成启动冒烟验证。

## 已知问题

- 工作区仍有本任务之前的大量未提交改动，未在本轮擅自清理或回退。
- 使用终端 Ctrl+C 结束启动器时，前端会收到中断并记录退出码 1；这属于测试结束方式，不是启动故障。直接启动安装版 Flutter 可执行文件已确认进程保持运行。
- 安装目录当前启动器测试进程保持运行，Bridge 端口为 `52741`；如需重新启动，先关闭现有 FaceSwap Studio 窗口即可。
- 仓库中面向旧 3.7.x API 的历史测试仍引用已移除的 `face_analyser`、`memory` 和旧临时文件接口；完整旧测试集会在收集阶段失败。本轮以当前 3.8.3 核心对应的 Bridge/worker/launcher 测试集为验收基线（49 项通过）。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话启动时先阅读本文件，再检查 Git 状态，从“下一步”继续。
