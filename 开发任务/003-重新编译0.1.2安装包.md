# 重新编译 0.1.2 安装包

状态：已完成
当前阶段：4/4
最后更新：2026-09-11 12:00

## 当前状态

当前分支为 `feat/facefusion-core-3.9.0`，应用版本为 `0.1.2`。本次将基于当前工作区重新完整构建 Flutter Web/Windows 前端、launcher、updater 和 Inno Setup 安装包，确保新包包含 FaceFusion 3.9.0 核心及现有前端、Bridge、更新链路改动。

旧安装包位于 `dist/installer/FaceSwapStudioSetup.exe`，大小 `1,442,439,720` 字节，生成时间 `2026-06-27 19:02:15`，SHA-256 为 `911D34F9E9B0F97A69974603F29C17E983026B32016169E317D172A7C6C43202`。构建脚本会覆盖这一可重新生成产物。

当前 `D:\Program Files\FaceSwap Studio` 安装版 Bridge 正在运行（PID 12448），与仓库 G 盘构建目录无文件占用冲突，本轮不关闭用户正在运行的安装版。

## 下一步

1. [已完成] 执行 Flutter analyze 和核心/Bridge 相关回归测试。
2. [已完成] 执行 `scripts/build_installer.ps1` 完整构建并保存构建日志。
3. [已完成] 校验 staging 内容、版本、核心版本、Web 入口、安装包时间/大小/SHA-256 和 PE 签名状态。
4. [已完成] 更新文档并提交构建记录；临时目录清理由环境策略阻止，已安全保留。

## 当前 TODO

- [x] 检查分支、工作区、版本、构建工具与磁盘空间
- [x] 记录旧安装包信息
- [x] 执行构建前静态检查与测试
- [x] 完整构建安装包
- [x] 校验安装包及 staging 内容
- [x] 更新文档与 Git 状态

## 最近验证状态

- 静态检查：Flutter analyze `No issues found`；Python `compileall` 通过；`git diff --check` 无错误
- 单元测试：核心/Bridge/worker/launcher 定向回归 `71 passed, 1 warning`
- 编译：Flutter Web/Windows、launcher、updater 和 Inno Setup 均成功；Inno `Successful compile (932.625 sec)`
- 运行测试：新部署运行包经正式 launcher 启动，Bridge 在线、FaceFusion `ready`、Flutter 前端存活，稳定观察 30 秒且无 Windows 崩溃事件
- 最近功能 Git commit：待提交本轮构建链修复与记录

---

## 任务目标

基于当前最新源码和工作区改动重新生成可分发的 Windows 完整安装包，并以实际产物检查证明安装包包含当前应用版本、FaceFusion 3.9.0 核心、Flutter 桌面端和 Web 工作台。

## 当前项目现状

- 仓库：`G:\\app\\facefusion\\faceswap-studio`
- 分支：`feat/facefusion-core-3.9.0`
- 应用版本：`0.1.2`
- 核心版本：`3.9.0`
- Flutter：`D:\\flutter\\bin\\flutter.bat`
- Inno Setup：`C:\\Program Files (x86)\\Inno Setup 6\\ISCC.exe`
- 安装包输出：`dist/installer/FaceSwapStudioSetup.exe`
- 工作区已有大量用户未提交改动，本次构建会包含其当前内容，但不会回退或擅自拆改。

## 技术方案

使用项目现有 `scripts/build_installer.ps1` 执行完整构建。脚本依次构建 Flutter Web/Windows、launcher、updater，创建 staging，复制核心、Bridge、Python venv/runtime、FFmpeg、桌面/Web 产物，然后执行 runtime repair 和 Inno Setup 编译。构建完成后从 staging 和安装包元数据双重验证，不以命令退出码作为唯一完成依据。

## 文件 / 模块清单

构建输入：

- `facefusion/`
- `faceswap studio/bridge/`
- `faceswap studio/flutter_app/`
- `.venv-win/`、`.runtime/ffmpeg/`
- launcher/updater 源码与构建脚本
- `installer/FaceSwapStudio.iss`

构建输出：

- `faceswap studio/runtime/windows_app/current/`
- `faceswap studio/flutter_app/build/web/`
- `启动FaceSwap Studio.exe`
- `dist/FaceSwapStudioUpdater.exe`
- `build/installer/app/`
- `dist/installer/FaceSwapStudioSetup.exe`
- 本轮构建日志

## 开发阶段

- [x] 阶段 1：环境与构建基线检查
- [x] 阶段 2：构建前验证
- [x] 阶段 3：完整安装包编译
- [x] 阶段 4：产物验收与记录

## 验收标准

- [ ] Flutter analyze 无错误。
- [ ] 核心/Bridge/worker/launcher 相关回归通过。
- [ ] `scripts/build_installer.ps1` 正常完成且 Inno Setup 报告成功。
- [ ] staging `VERSION` 为 `0.1.2`、核心版本为 `3.9.0`。
- [ ] staging 同时包含 Windows 前端、Web `index.html`/`main.dart.js`、Bridge、launcher、updater 和 FFmpeg。
- [ ] 新安装包时间戳晚于旧包，大小合理且计算 SHA-256。
- [ ] 不破坏用户已有未提交源码改动。

## 已完成内容

- 检查 Git、构建脚本、Flutter、Inno Setup、Python、FFmpeg 和磁盘空间。
- 记录旧安装包信息和当前安装版运行状态。
- 完成 Flutter analyze、Python 编译和 71 项定向回归。
- 修复 Flutter 镜像配置无法被调用方覆盖的问题；国内 Pub 镜像返回 424 时改用官方源完成构建。
- 完成 Flutter Web/Windows、launcher、updater、staging runtime repair 和 Inno Setup 编译。
- 新安装包：`1,442,517,301` 字节（`1375.69 MiB`），生成时间 `2026-09-11 11:54:57`，SHA-256 `28014C0424251976F520C8A9835B92212B8F94BD2594C643180B102541173D0A`。
- staging 验证：`0.1.2`、FaceFusion `3.9.0`、29,836 个文件；Bridge 导入、桌面端、Web、launcher、updater、FFmpeg 和 Python 均通过检查。
- Web 验证：`base href=/studio/`、包含端口 `52741`、不含旧端口 `50941`。
- 新构建运行包完成正式 launcher 启动与稳定性冒烟。

## 当前关键修改

- `scripts/common.ps1`：允许显式环境变量覆盖默认 Flutter 国内镜像。
- `避坑指南/Flutter国内镜像424导致安装包构建失败.md`：记录网络故障根因和解决方案。
- `dist/installer/FaceSwapStudioSetup.exe`：已重建（被 `.gitignore` 排除）。
- `dist/installer/build-0.1.2-20260911.log`：完整构建日志（被 `.gitignore` 排除）。

## 已知问题

- `pytest tests` 有 6 个升级前遗留的旧 API 测试收集错误，本轮采用已验证的核心/Bridge/worker/launcher 定向回归作为门禁。
- 安装包 Authenticode 状态为 `NotSigned`，与项目现有未签名构建方式一致；用户运行时仍可能看到 SmartScreen/未知发布者提示。
- Inno Setup 提示 `x64` architecture identifier 已弃用，以及 Flutter Windows 构建有 media_kit CMake 开发警告，二者均未阻断本次构建。
- 环境安全策略阻止递归删除 `build/installer/app`、Flutter Windows 中间构建和 `.dart_tool/flutter_build`；没有执行绕过式删除，约 4GB 可重建文件暂时保留。

## 开发日志

- 2026-09-11：启动完整安装包重编译任务，完成构建基线检查。
- 2026-09-11：构建前验证通过，进入完整安装包编译。
- 2026-09-11：国内 Pub 镜像返回 424；完成镜像可覆盖修复并改用官方源。
- 2026-09-11：完整安装包构建与 staging/运行时验收通过。
- 2026-09-11：新运行包稳定性验证通过，测试进程与端口已清理；任务完成。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

新会话先阅读项目规则与本文件，检查 Git 状态，从“下一步”继续。
