# 常用模型离线安装即用与移除核心更新

状态：已完成。
下一步：用户运行最终安装包；核心后续通过全量包升级。
分支：`fix/remove-core-updater`
最近提交：`3bf5b3e`，源码提交已推送至 `origin/fix/remove-core-updater`。
阻塞：无；工作区保留此前用户修改，按任务范围暂存。

## 目标与验收

- [x] 删除应用内 FaceFusion 核心版本检查、下载、解包覆盖能力及 HTTP 接口。
- [x] 移除 Flutter 更新弹窗中的核心更新状态、按钮和客户端调用。
- [x] 保留应用更新；核心后续通过维护者发布全量安装包升级。
- [x] 常用模型清单包含 14 个 ONNX 和 14 个 hash，共 1,807,644,740 字节。
- [x] 保留 Python、venv、FFmpeg 与 FFprobe 离线打包链。
- [x] 更新相关与离线模型、launcher、Web 测试 23 项通过；队列、作品、工作台、角色库测试 29 项通过。
- [x] Flutter lib 分析无问题；Windows、Web、launcher、updater 编译成功。
- [x] Inno Setup 生成 0.1.3 安装包，编译耗时 1326.219 秒。
- [x] 打包后的 Bridge 独立端口启动、模型 ready、移除接口返回 404、FaceFusion CLI 成功；核心 WebUI 在 7869 就绪，pip check 通过。
- [x] 记录最终产物校验信息，源码已提交推送。

## 实现

`faceswap studio/bridge/app_server.py` 删除独立核心升级的状态、查询、下载、预检、备份和覆盖方法；应用更新不再访问 FaceFusion 上游。
`faceswap studio/flutter_app/lib/shared/widgets/studio_update_dialog.dart` 删除核心更新界面与交互，仅保留应用更新。
`faceswap studio/flutter_app/lib/shared/services/bridge_client.dart` 删除核心更新请求。
`tests/test_app_server_updates.py` 将核心覆盖测试替换为已移除接口及仅检查应用发布的回归。
`installer/core-models.txt`、`scripts/validate_offline_models.py` 和原有打包脚本负责离线模型、校验及完整运行时。

## 验证记录

2026-09-11：完成 14 个模型 CRC 校验，补齐 FFprobe，venv 对齐 FaceFusion 3.9.0 CUDA 12；干净 PATH 下启动及真实图片处理通过。用户要求暂停最终打包，留下 `.partial`，该文件不是可交付安装包。
2026-09-15：收到移除核心更新并重编译指令，恢复本任务。52 项针对性回归通过，Flutter `dart analyze lib` 无问题。Windows/Web/启动器/更新器编译完成。

## 已知边界

- `alphaface_256`、`hrffa` 不在常用离线集合内，选择时仍需按需下载。
- 历史全库测试有旧 API 收集故障，本轮使用覆盖本次变更的定向回归。
- 已有运行实例占用 52741/7860，使用独立端口 52749 验证新 staging，不停止用户会话。
- 工作区大量既有 UI、配置和构建修改保持原样，不混入无关提交。

## 接力信息

[CODEX_LONG_TASK_CONTINUE_V3]

继续使用本文件。构建日志：`build/remove-core-installer.log`；验证脚本：`build/verify_removed_core.py`；最终安装包：`dist/installer/FaceSwapStudioSetup.exe`。

2026-09-15 09:42：运行验收完成，常用模型压缩完成，正在压缩 Python/FFmpeg/venv。源码提交 `3bf5b3e` 已推送。测试使用的 `GRADIO_SERVER_PORT=7869` 仅用于隔离端口，不修改应用配置。

## 最终交付

- 安装包：`dist/installer/FaceSwapStudioSetup.exe`
- 版本：0.1.3；FaceFusion 3.9.0。
- 大小：2,912,330,177 字节（2.71 GiB）。
- 生成时间：2026-09-15 09:55:33 +08:00。
- SHA-256：`F98F5BCD2A953C79943E18FA6CA86687E3C0F43EC3093E16DC5EF025456D7F95`。
- 校验文件：`dist/installer/FaceSwapStudioSetup.exe.sha256`。
- 安装说明：`dist/installer/安装说明-0.1.3.md`。
- 最终 staging 的 28 个模型文件再次通过 CRC 校验，合计 1,807,644,740 字节。
- 验证范围：编译器成功产物、staging 模型完整性、隔离 PATH 的 Bridge/CLI/核心 WebUI 启动、pip check；未覆盖安装到用户当前系统，以免影响已有运行实例。
- 当前任务源码提交：`3bf5b3e`，已推送。既有非本任务改动继续留在工作区。
